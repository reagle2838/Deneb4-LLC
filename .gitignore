# dependencies
/node_modules
/.pnp
.pnp.*

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem
Thumbs.db
desktop.ini

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# env files
.env
.env*.local

# typescript
*.tsbuildinfo
next-env.d.ts

# vercel
.vercel

# uploaded client files (kept on the server, not in git)
/public/uploads/*
!/public/uploads/.gitkeep

# Claude Code local settings + personal hooks (not shared)
.claude/settings.local.json
.claude/hooks/

# Agent heartbeat run log + dedup state (ephemeral, regenerated at runtime)
/content/admin/agent-runs.yaml
/content/admin/agent-state.yaml

# Builder: per-client assembled build artifacts (regenerated from configs)
/builds/
