# Deneb4 Autonomy Roadmap

**Vision:** Deneb4 runs as a mostly autonomous studio. A small team of agents handles intake, provisioning, assembly, QA, client communication, billing, handoff, and maintenance. Ridhi's required role shrinks to: discovery calls (when a client requests one) and the human approval gates below.

**Keystone principle:** every autonomous step is only as trustworthy as the verification harness underneath it. The test suite that can answer "is this assembled site correct?" is the load-bearing wall. Build it early (Phase 3), not last.

**Working note:** internal agent communication lives in the Workspace at `/cms-admin` (Agents tab), backed by per-client ledger files and the `/api/agents/ledger` API.

---

## Architecture decision (2026-07-09): git-native, incremental Builder

Confirmed by Ridhi, supersedes the "regenerate from a config" model that Builder v1 proved:

- **Each client's website lives in its own GitHub repo**, not a local `builds/<slug>/` folder that gets deleted and regenerated. The repo IS the site; git history IS the project's daily progress log.
- **The Builder does not build from scratch.** For a website request, it clones/pulls the **existing template repo** (name TBD, Ridhi to provide) and builds on top of it per the client's spec, the template repo is the real starting point, not `templates/starter/` (which only proved the assembly mechanics on a placeholder).
- **Work incrementally, like a person would:** pull the current state of the client's repo → see what's already there, including any manual edits Ridhi made → make the specific change being asked for → commit → push. Never a blind wipe-and-regenerate. This is also what fixes the "Ridhi hand-edits something and it survives" problem: a manual edit is just a commit, and the next agent run starts from that commit, not from a fresh copy.
- **"Continue work each day until finished"**: an agent resuming a project just means pulling that client's repo, checking progress against the pipeline stage + ledger, and continuing. Same durable-state pattern already used for conversations (the ledger) and project stage (the pipeline), now extended to the actual code.
- **Consequence for QA (Phase 3):** the verification harness will need to handle whatever the real template's build/serve story is (this is unknown until the template repo is shared — could be a static build, could need `next build`/`next start`, etc.), not only the static-stub model `verify.mjs --manifest` was proven against.
- **Blocked on:** the template repo (name/access, Ridhi to provide — see Phase 1), and a GitHub API credential so agents can create/clone/commit/push per-client repos (see Phase 4). Builder v1's deterministic engine (module toggles, theme, fact-slot copy, QA handoff, escalation) is not wasted, it's the config layer that will sit on top of the git workflow once both land.

## Architecture decision (2026-07-09): Google Workspace intake

Confirmed by Ridhi: client specs come from a **Google Form** plus several supporting **Google Docs** the client fills out, not a custom-built form on the Deneb4 site. Agents (and/or Claude Cowork) need read access to the form responses and the associated docs. This replaces the earlier "60-question checkbox form built in-house" framing in Phase 2 below. Ridhi will provide the specific form/docs and Google Workspace access details later.

## Note: Claude Cowork's role in the flow

Confirmed by Ridhi: Claude Cowork (a supervised, interactive Claude Code session, not an unattended agent) is the designated path for anything the deterministic agents can't or shouldn't handle on their own: **structural requests** (already a permanent human gate per Phase 0), **one-off manual overrides** Ridhi wants to make herself, debugging something QA didn't anticipate, and building out new template modules for the catalog. It works directly against a client's real repo once that exists (see the git-native Builder decision above). This formalizes what was already implicit: agents handle the repeatable work, Claude Cowork handles the judgment calls. (To do later: reflect this explicitly in `docs/agents.md` as the structural-request escalation path.)

## Architecture decision (2026-07-13): deterministic assembly kept; visual variety via multiple template SETS

Confirmed by Ridhi, after explicitly considering and rejecting an AI-generates-code-per-client model:

- **Assembly stays deterministic.** The Builder remains config-in, known-output-out. This is deliberately protected because every safety mechanism already built depends on it: the fact-slot rule (agents never assert unsupplied facts), QA's reliability (it verifies a *bounded, known* output space), the change loop's revert safety net (re-run the same assembler, diff old-vs-new), and fixed per-module pricing. An AI freely writing per-client code would reopen all four. Off-menu/structural work remains the permanent human gate → Claude Cowork (Phase 0, unchanged).
- **Visual variety is solved by having many template SETS to choose from, not by non-deterministic building.** A "set" is a complete, visually distinct family: one website shell + one CMS admin core + one implementation of each feature module (catalog/careers/blog/gallery), all sharing a single strong visual identity, but technically identical in contract to every other set. A client picks a set; the same deterministic pipeline builds it.
- **How a set is chosen:** the intake form's existing "which visual direction fits you best?" question maps to a set (a deterministic *lookup*, not generation) instead of, as today, only a color preset inside one shared template. Today two clients who pick the same modules get the same layout with different accent colors; with sets they get genuinely different-looking sites.
- **The stitching contract:** each template (shell, CMS core, each module) ships a `manifest.json` whose `capability` field is STABLE across all sets (always `catalog`/`careers`/`blog`/`gallery`/`site`/`cms-core`) and whose `set` field ties a family together. An agent asks for "the catalog module for this client's set" via `capability` without knowing set internals. **Data shapes (Job/Article/Product/CatalogCategory/GalleryImage) are frozen identical across every set** so content-seeding keeps working unmodified. Route/nav ids stay identical in meaning across sets (`/catalog` everywhere).
- **Template vs functionality split (for outsourced set-building, e.g. ChatGPT Codex):** the set author builds the VISUAL + STRUCTURAL layer only — pages, components, admin editor UIs, forms, all UI states, typed data interfaces, and clearly-marked async *seams* with mock data — but NOT the real backend. Persistence, auth, email, and uploads are seams the Deneb4 assembler wires at stitch time. This is what let the **persistence decision (now resolved, see 2026-07-15 below: Turso via `d4-cms-core`) swap in without touching any set** — the set author stays persistence-agnostic; async seam signatures don't change when the network backend changes. Each set must run standalone on mock data with zero credentials so its design can be reviewed.
- **Non-negotiable for any new set** (or the existing QA harness auto-rejects the assembled site): passes axe-core with zero serious/critical violations incl. WCAG-AA contrast, no horizontal scroll at 375px, a designed empty state for every data view, renders with no env vars set, respects `prefers-reduced-motion`.
- **Our-side follow-up when a new set actually lands** (not blocking the set author): register it in `registry.json`, teach the intake→set mapping and the pricing engine that the set exists. Small, known work.

## Architecture decision (2026-07-15): client-site hosting is Vercel + Turso + Vercel Blob

Confirmed by Ridhi. Supersedes the earlier DigitalOcean-droplet-plus-local-JSON-files direction (that plan is dead; nothing was built against it besides the pricing analysis).

- **Why not a raw VPS (DigitalOcean/Hostinger Droplet):** correctly flagged as real ops toil that fights the zero-touch goal — manual Nginx, PM2, Certbot/SSL renewal, OS patching, an agent one Nginx-config typo away from bricking every client site on the box.
- **Why not plain Vercel/Cloud Run/DO App Platform (as first proposed as the fix):** all three are stateless — no persistent writable disk between deploys/instances — which breaks `d4-cms-core`'s filesystem-based JSON data store outright. That gap is real, not a caveat to hedge around.
- **The actual fix: Vercel (hosting/deploy) + Turso (data) + Vercel Blob (uploads).** Turso is libSQL — a real database with an HTTP driver serverless functions can use, and (unlike Supabase's free tier) built on always-on edge replicas with no pause-after-inactivity model. Vercel Blob replaces local file writes for uploaded images/PDFs. Together they give an actually-stateful app on an actually-stateless host, with zero server to ever patch, SSH into, or restart.
- **Cost:** Vercel Pro $20/mo flat (Hobby is non-commercial-use only). Turso free tier (100 DBs, 500M row reads/mo, 10M row writes/mo, 5GB storage) comfortably covers dozens of low-traffic B2B client sites before any usage-based cost kicks in. Vercel Blob: 1GB free, then $0.023/GB. Realistic total today: **$20/month**, cheaper than the droplet plan it replaces, with no ops burden at all.
- **Done (2026-07-15):** `d4-cms-core`'s `data-store.ts` rewritten from sync fs-JSON to async libSQL (Turso in production via `TURSO_DATABASE_URL`/`TURSO_AUTH_TOKEN`, a local SQLite file in dev with zero credentials needed — same code path both ways); the upload route rewritten to use Vercel Blob when `BLOB_READ_WRITE_TOKEN` is set, falling back to local disk in dev. All four content modules (catalog/careers/insights/gallery) and their call sites converted to the now-async data layer. `d4-site-template`'s contact-form fallback migrated off raw fs the same way. Verified end-to-end: assembled a throwaway 5-module client, `npm install`, clean `tsc --noEmit`, clean `next build`, live server on the local SQLite fallback answering real reads (`/api/careers`) and writes (`/api/careers/apply`) that persisted to `data/cms.db`.
- **Feature gating for clients who don't want the full module set:** already solved — the existing `modules[]` array in each build config plus `registry.json`'s closed menu means an unwanted module (e.g. careers, gallery) is simply never assembled into that client's build. No runtime toggle layer needed or planned; a client never receives code for a feature they didn't select.
- **Still open, [Ridhi]:** create a Vercel account + project linked to the `deneb4admin` GitHub org (or wherever client repos live), a Turso account + one database + auth token, and enable a Blob store on the Vercel project — needed before any real (non-dev-fallback) client deploy, not before now.

---

## Phase 0: Decisions (owner: Ridhi)

| # | Decision | Status |
|---|---|---|
| 1 | **Catalog boundary.** Everything offered is pre-templated; nothing off-menu. Product/Service Catalog module rule: up to **50 pre-seeded catalog items** included (editable by client afterward). Seeding beyond 50 is an **upcharge**. Clients can add unlimited items themselves **only if their package includes the CMS with catalog management**. Full module menu: see `docs/module-catalog.md`. | DECIDED, menu APPROVED 2026-07-07 |
| 2 | **Disclosure stance.** Honest, never deceptive. Some internals may be left unsaid, but nothing stated may mislead. Copy and client comms must survive the question "is this literally true?" | DECIDED |
| 3 | **Human gates.** Kept: scheduled appointments, start-approval, design review, copy review, structural requests, payment, handoff. Relaxation plan below, thresholds confirmed by Ridhi. | DECIDED |
| 4 | **Pricing/packaging.** Deferred. Revisit after the catalog menu is approved. | DEFERRED |

### Gate map and relaxation plan (thresholds confirmed by Ridhi, 2026-07-07)

| Gate | Who holds it today | Proposed relaxation trigger | End state |
|---|---|---|---|
| Scheduled appointments (discovery calls) | Ridhi | Never: this is the human face of the business | Human, permanent |
| Start-approval (new client enters pipeline) | Ridhi | Never: commits scope and money | Human, permanent |
| Design review (before client sees staging) | Ridhi | 10 consecutive builds approved with zero visual edits AND visual-regression suite green | Spot checks |
| Copy review | Ridhi | 10 consecutive builds with zero fact corrections, with fact-slot enforcement live (bots may only assert facts the client supplied) | Spot checks |
| Structural requests (anything out of catalog) | Ridhi | Never: out-of-catalog equals new engineering | Human, permanent |
| Payment (final invoice decision) | Ridhi | Never: automation sends and detects, human decides | Human confirms, permanent |
| Handoff (credentials doc + ownership transfer) | Ridhi | 5 consecutive clean handoffs with the secure secrets flow proven | Spot checks |

---

## Phase 1: Product core (the moat)

- [x] **[Ridhi]** Template ecosystem DELIVERED 2026-07-09 as seven public GitHub repos under `deneb4admin`: **d4-site-builder** (the assembler: manifest schema, registry.json closed menu, deterministic wiring), **d4-site-template** (Next 15/React 19/Tailwind base with nav slot system + theme presets), **d4-cms-core** (/admin auth + TOTP, JSON data store, uploads, panel registry), and four feature modules (**d4-careers-portal**, **d4-insights-blog**, **d4-catalog**, **d4-gallery-editor**). Production-grade: no leftover branding, builds clean, modules declare routes/nav/adminPanels/env via manifests.
- [x] **[Claude]** Verified the base template builds to production grade (assembled site compiles + type-checks clean, 12 routes) (2026-07-09)
- [x] **[Claude]** `docs/module-catalog.md` aligned with the real d4 registry (2026-07-10): registry.json is the operational closed menu; the doc now describes the 7 shipped repos + future module candidates (campaign tracker, support intake, update requests, RFQ, team page)
- [x] **[Ridhi]** Modules exist as versioned, toggleable components with manifests (the d4 repos)
- [x] **[Ridhi]** Menu-driven theming: 3 presets (slate-teal, warm-sand, ink-indigo) + custom palette support in the assembler
- [ ] **[Claude]** Fact-slot copy enforcement for generated/refined copy (the d4 build config carries identity fields verbatim today; applies when the LLM copy layer activates)

## Phase 2: Intake-to-build compiler

- [x] **[Ridhi]** Intake mechanism decided: a **Google Form** plus several supporting **Google Docs** the client fills out (not a custom in-house form). Specific form/docs + Google Workspace access — Ridhi to provide later.
- [ ] **[Claude]** Google Workspace read access for agents/Claude Cowork (Forms responses + Docs) — see Phase 4
- [ ] **[Both]** Answer-to-config mapping rules: every form/doc field maps to a concrete, testable build operation
- [ ] **[Claude]** Config schema (extends the Builder v1 `BuildConfig` shape already proven)
- [x] **[Claude]** Brand ingestion, palette (done + verified E2E 2026-07-12): `npm run brand -- <slug> --url <their-current-site>` fetches the page + same-origin stylesheets, weights every color literal, and derives a complete 7-token custom theme (chromatic colors compete for accent, light neutrals for backgrounds, dark for text; missing roles derived so the palette is never partial). It files the palette as a **change proposal** — the Workspace panel renders the actual swatches, and approving applies `config.theme` (superseding the preset) through the normal change loop with full QA. Validator extended: theme patches must be all 7 known tokens in "R G B" channel form, all-or-nothing. Verified against the Deneb4 site itself: extracted the real #006b8f brand, approved, theme.css updated in the client repo, QA green incl. axe on the new palette. Fonts/logo ingestion remain future work (fonts aren't config-level in the template yet; logos need a human eye).
- [ ] **[Claude]** Content seeding note: `npm run seed -- <slug> --file <csv|json|txt> --type jobs|articles|products|galleries` parses client material into the exact d4 module data shapes, STAGES it for review (`content/admin/seeds/`, gitignored), and `--apply` merges into `builds/<slug>/data/` by id/title without ever clobbering client-authored content; products auto-derive their catalog categories. Unstructured text extraction is ANTHROPIC_API_KEY-gated (facts only, still human-reviewed). Verified E2E 2026-07-12 (CSV jobs + JSON products, dedupe on re-apply, seeded jobs rendering on /careers). Remaining: images (uploads need manual placement) — this box stays open for the Google-Form-driven intake compiler below.
- [ ] **[Claude]** The compiler: parsed intake in, build config out, feeding the git-native Builder (see architecture decision above)

## Phase 3: Verification harness (keystone)

- [x] **[Claude]** Site verification harness `scripts/verify.mjs` (+ `npm run verify:site <url>`): dependency-free checks against a running site (critical routes render, auth gates behave, page structure = title + h1, internal-link crawl, key assets), machine-readable pass/fail + exit code, optional QA-ledger reporting (`--client --key --report-to`). This is the QA agent's tool + a regression guard for Deneb4 itself; points at a client staging URL later. (done)
  - **It immediately caught a real, pre-existing bug** (see note below).
- [ ] **[Claude]** Per-module test suites (needs the module catalog / template)
- [x] **[Claude]** Browser-dependent checks (2026-07-11): `verify.mjs` now runs real-browser checks via Playwright (installed in THIS repo only; client builds never need it), enabled automatically when the Builder passes `--qa-dir` (or manually with `--browser on`). Four new checks: **browser errors** (console errors, page exceptions, failed same-origin requests per route), **accessibility via axe-core** (real rendering; critical/serious violations fail QA, which includes real color contrast), **mobile overflow** (no horizontal scroll at 375px), and **visual snapshots** (full-page screenshots per route, pixel-diffed against a last-known-good baseline; informational, not a gate, since intentional changes change pixels; the changed-page list goes into the ledger summary as evidence for Ridhi's review, and the baseline refreshes only when the whole battery passes). QA artifacts live at `builds/.qa/<slug>/` (baseline/current/diff), outside the client repo so its git history stays clean. Verified E2E on acme-demo: green change build (visual changes correctly listed per page), fault-injected red build (browser errors caught the bogus route, change auto-reverted, baseline untouched).
- [x] **[Claude]** QA adapted to the real templates: the Builder derives the verify manifest from the assembler's per-site `d4.assembly.json` (routes by owning module), builds + serves the real Next app, and runs the harness against it. Dynamic routes (`/insights/[id]`) and deep admin pages are skipped until content seeding/session support exist. (2026-07-09)

> **✔ RESOLVED (2026-07-11) — FINDING (2026-07-08, from the harness): Keystatic content was silently half-broken on the live site.** Root cause confirmed: with `path: 'content/x/*'` (no trailing slash), Keystatic expects entries as `content/x/<slug>.yaml` with linked files in `content/x/<slug>/`; the hand-written entries were stored as `<slug>/index.yaml` (the trailing-slash layout) and were invisible to the reader. The Eagle Engineering case study yaml additionally had an unquoted `: ` in its summary, making it invalid YAML outright. Fixed by a one-time migration matching the canonical layout of the one CMS-saved article exactly: moved each `index.yaml` up to `<slug>.yaml`, flattened `title: {name, slug}` to the plain name string (the slug is the filename), fixed the invalid summary with a block scalar. Verified: the reader lists all 6 articles + the case study, `/work/eagle-engineering` renders 200, all internal links green. Future entries saved through `/keystatic` were always in the correct format and are unaffected.

> **⚠ FINDING (2026-07-11, from the new browser checks, run against the Deneb4 site itself): two design tokens fail WCAG AA contrast sitewide.** `--text-faint: #7d8d9b` (~26 nodes/page on the light backgrounds, ratio ~3.2 vs required 4.5) and `--accent-light: #1f93ba` as text on white (ratio ~3.5). Closest same-hue compliant values: `#65727e` (4.59:1) and `#1a7a9a` (4.89:1) in `src/app/globals.css`. Also `link-in-text-block` on `/contact` and `/start` (links distinguishable only by color). These change the site's look, so they are **Ridhi's design decision, not auto-fixed**. Note: the d4 client template passes the same gate cleanly, so client sites are unaffected. A `/process` mobile overflow (fanned card deck pushing past a 375px viewport) WAS fixed, since that was a layout bug, not a design choice.

## Phase 4: Infrastructure and credentials

- [x] **[Claude]** Staging deploy automation (done + verified E2E 2026-07-12): `npm run deploy -- <slug> [--stop]` with two providers. (1) **Host seam** `STAGING_DEPLOY_CMD` — any real host plugs in with one shell command (run with D4_SLUG/D4_OUT_DIR; the last URL it prints is recorded); nothing else changes when the host decision lands. (2) **Local provider** (default, zero credentials): builds + runs the site as a persistent background process on a per-client port (4200-4299), records `http://127.0.0.1:<port>` on the client record via the new `/api/agents/staging` writeback route — a real target for internal review today, monitored by the uptime watch. Builder auto-deploys after green builds when `AUTO_DEPLOY_STAGING=true`. Verified: deploy → 200 → recorded status ready; stop → killed → recorded down.
- [ ] **[Both]** Deploy pipeline DECIDED 2026-07-15: Vercel. Wire the actual deploy command (Vercel CLI/API) into `STAGING_DEPLOY_CMD` once Ridhi's Vercel account/project exists (see 2026-07-15 architecture decision above).
- [ ] **[Claude]** Production deploy + DNS handover automation
- [ ] **[Ridhi]** Re-enable CMS auth (delete `CMS_AUTH_DISABLED` from `.env.local`) when testing ends
- [ ] **[Claude]** Scoped agent credentials, secrets store, rotation policy (Google, Vercel, Wave, CMS, git)
- [ ] **[Ridhi]** GitHub credential for agents: a token (or GitHub App) that can create/clone/commit/push **per-client repos**. Also decide where those repos live — a dedicated org, or Ridhi's personal account for now (open question from 2026-07-08 chat).
- [ ] **[Ridhi]** Google Workspace credential for agents/Claude Cowork: read access to the intake Google Form's responses and the supporting Google Docs (Ridhi has Google Workspace; specifics to follow)
- [ ] **[Both]** Hook up Supabase (noted 2026-07-08 by Ridhi; scope/purpose not yet defined, e.g. replacing flat YAML storage for clients/leads/tasks/ledger, or something else — clarify before building)

## Phase 5: Agent system and shared workspace

- [x] **[Claude]** Shared per-client agent ledger + Agents tab in Workspace + `/api/agents/ledger` (done)
- [x] **[Claude]** Per-client pipeline state machine: 9 stages in `src/lib/pipeline.ts`, `pipeline` field on Client, single-writer `/api/agents/pipeline` (idempotent, every transition auto-logged to the client's ledger channel), PipelinePanel in the command center + roster labels (done)
- [x] **[Ridhi]** Agent contracts in `docs/agents.md` APPROVED 2026-07-08
- [x] **[Claude]** Agent heartbeat: `/api/agents/tick` (POST = run all sensing duties, GET = run history). Duties run isolated (one failure never sinks the rest), each run recorded to a run log (`agent-runs.yaml`), once-per-day dedup via `agent-state.yaml`. Live duties: calendar digest + client worklist (unread messages + pending drafts + projects at a gate). form-received/payment-received stubbed pending Drive/Wave. Agents tab is now a control surface: heartbeat status + per-duty outcomes, a "Run heartbeat now" button, and a readiness strip (Email/Calendar/Agent-key wired vs pending, plus a CMS-auth-disabled warning). (done)

> **Functional verification (2026-07-08):** the whole agent system + portal + workspace was exercised against a real `npm run build` production server: all marketing routes 200, portal/portal-feedback gate correctly (307), cms-admin renders, and every agent flow works end to end (ledger, pipeline transitions, heartbeat + worklist, draft-reply gate with verified portal/widget non-leak + approve, onboarding + welcome email, alert escalation). All three notification emails fired (console fallback) with zero runtime errors in the prod log.
- [x] **[Claude]** Scheduler plumbing (2026-07-12): `scripts/heartbeat-trigger.mjs` (reads the agent key itself, POSTs the tick, exits cleanly when the app is offline — verified both paths) + `scripts/register-heartbeat-task.ps1` to register/remove a Windows Scheduled Task ("Deneb4 Agent Heartbeat", default every 30 min, logs to builds/.heartbeat.log).
  - **[Ridhi]** One command when ready to stop clicking "Run heartbeat now": `powershell -ExecutionPolicy Bypass -File scripts\register-heartbeat-task.ps1` (registering a system task is deliberately left to you).
- [x] **[Claude]** Escalation, part 1: any agent posting a `kind: alert` ledger entry emails Ridhi immediately (the alert-and-stop rule from docs/agents.md) (done)
- [x] **[Claude]** Error handling, part 2: tick duties are failure-isolated and every run is logged; any duty error escalates to Ridhi's inbox (done)

## Phase 6: Pipeline stages (ops layer; many independent quick wins)

- [x] **[Claude]** Calendar check endpoint: `/api/agents/calendar-check` (GET preview, POST = email digest + Studio ledger entry; reads the calendar's secret iCal address, no OAuth) (done)
- [ ] **[Ridhi]** Paste the calendar's "secret address in iCal format" into `GOOGLE_CALENDAR_ICS_URL` in `.env.local` (instructions in the file)
- [ ] **[Both]** Pick the daily trigger: a scheduled cloud job or Vercel Cron hitting the endpoint in production, Windows Task Scheduler locally, or a scheduled cloud agent
- [x] **[Claude]** Provisioning, part 1: client creation posts a birth record to the ledger, and portal credentials can be emailed to the client on create and on password reset (opt-in checkboxes; falls back to shown-once + manual share until Resend is configured) (done)
- [ ] **[Claude]** Provisioning, part 2: Drive folder creation + intake doc sharing (needs Google Drive API credentials)
- [x] **[Claude]** Intake parsing (done + verified E2E 2026-07-12, all the way to a live site): `npm run intake -- --file <google-form-export.csv> [--row N]` maps the form's answers onto a PROPOSED build config with fuzzy header matching (business name/tagline/description/contact fields; theme wording → nearest preset; features answer → modules — and ONLY the features answer, per the fact-slot rule; an existing-site URL becomes a brand-ingest suggestion). Unmapped fields are listed, never invented. Ridhi reviews the staged proposal, `--apply` writes the build config (refuses to clobber an existing one). Verified: two synthetic form responses parsed correctly after two test-caught fixes (a "shop" in an email address must not add the catalog; the theme question must not be double-used as the features answer), then Meridian Tool & Die went CSV row → config → client → **full build, QA green, internal review** untouched by hand.
  - Google API integration (reading the Sheet directly instead of a CSV export) remains blocked on Google credentials.
  - **Aligned to Ridhi's REAL Google Form (2026-07-14):** the form is live (18 sections, ~90 questions, conditional branching). The parser was reworked to map by the form's stable per-question **codes** (Q3-1, Q7A-1, ...), which Google uses verbatim as CSV headers — exact and reword-proof — with a keyword fuzzy fallback and a printed **mapping report** (matched-by-code / matched-by-fuzzy / not-found) so nothing is ever silently dropped. Modules now come from the four explicit Yes/No decision questions (Q7A-1 catalog, Q8A-1 careers, Q9A-1 blog, Q10A-1 gallery), not an inferred checkbox. Decisions wired: "not sure" on a module → left OFF + flagged (never bill for an unrequested module); Q11 custom functionality = Yes → flagged as a **structural request** (off-menu, needs Ridhi + a quote, never auto-built); catalog size 51+ (Q7B-1) → the 50-item upcharge flag; theme "match existing brand" → brand-ingest flag, "minimal/neutral" → slate-teal + note. Beyond the config it also extracts the **client-contact record** (Q4), decision **flags** (structural/upcharge/discovery-call/GitHub-transfer/brand-URL), and a **context bundle** of the rich answers (story, customers, trust signals, categories, content readiness) for Ridhi / the About page / seeding. Verified against a synthetic CSV built from the exact form titles: 21/21 fields matched by code, all three decisions correct; fuzzy fallback verified by stripping codes off two headers (caught + reported, not silent). The form's exact CSV headers confirm on the first real dogfood export.
- [x] **[Claude]** Build orchestration loop, engine proof (BUILDER AGENT v1, on a STUB template): deterministic assembly engine (`scripts/builder/*.mjs`) + runner (`scripts/build-client.mjs`, `npm run builder`). Takes a hand-authored build config (`content/build-configs/<slug>.json`), assembles a static site from `templates/starter/` into `builds/<slug>/` (config-level: toggle modules, apply theme, inject supplied facts; fact-slot rule enforced, never fabricates), serves it locally, runs the template-aware QA harness (`verify.mjs --manifest`), and on green writes a change summary to the client ledger + advances pipeline to `internal-review` and STOPS at Ridhi's gate; on validation/QA/exception failure it escalates (alert email) and stays at `building`. Hybrid: optional key-gated LLM copy layer (`ANTHROPIC_API_KEY`) that only rephrases supplied copy. Verified end-to-end (green, gated-stage refusal, both escalation paths, idempotency, regression-safe QA edit, tsc clean). (done)
  - **Swappable stub:** `templates/starter/` + `template.json` is a placeholder proving the assembly mechanics (config validation, module toggling, theme, fact-slot copy, ledger/pipeline/escalation wiring). Those mechanics carry forward.
  - To run the demo: create a client at pipeline `building`, then `npm run builder -- <slug> --report-to http://localhost:3005` (example configs: `content/build-configs/acme-demo.json` valid, `broken-demo.json` invalid).
- [x] **[Claude]** BUILDER AGENT v2, git-native, driving the REAL d4 ecosystem (done 2026-07-09, verified end-to-end): `npm run builder -- <slug> --report-to <origin>` mirrors the 7 d4 repos into `vendor/d4/` (recording exact commit SHAs to the ledger), runs Ridhi's `d4-site-builder` assembler with the client's config (`content/build-configs/<slug>.json`, d4 format), git-inits the output as the client's own repo (initial commit + lockfile commit), npm-installs + `next build`s + serves the real app, QAs it via the derived manifest, and on green posts the change summary + advances to `internal-review` and STOPS at Ridhi's gate; failures escalate (assembler's own closed-menu enforcement verified: unknown module → alert email → pipeline stays at building). Existing builds are NEVER wiped (re-run refuses; incremental changes come via the future change loop). GitHub push layer written but **dormant until GITHUB_TOKEN + GITHUB_OWNER are set** (then: private repo `d4-client-<slug>` auto-created + pushed). v1 stub engine + `templates/starter/` deleted (superseded).
  - Live demo: client `acme-demo` (Acme Fabrication) assembled at `builds/acme-demo` — a real git repo with the site; Ridhi can delete the client + folder when done exploring.
  - The incremental change loop is now DONE (see the change-implementation item below). Remaining Builder-adjacent work: the Comms agent producing change-lists/config edits from client comments (see the Comms item below).
- [x] **[Claude]** Draft-reply gate (the copy-review human gate): agents propose replies via `/api/clients/feedback` action `draft` (x-agent-key allowed); they live in a separate `draftReplies` array that never reaches the portal or widget; Ridhi approves/edits/rejects in the Workspace MessageThread; approving sends the reply + emails the client. Agents can propose but only Ridhi (CMS session) can approve. Pending drafts surface in the daily worklist. (done)
- [x] **[Claude]** COMMS AGENT (done + verified E2E 2026-07-11): triage of new client messages on its stages (client-review, approval), running as a heartbeat duty (`comms-triage` in `/api/agents/tick`, per-message dedup so any cron frequency is safe). Per batch of new messages it produces: a triage summary on the client's ledger (each message classified change-request / question / structural / approval / urgent / unclear, with reasons); at most ONE draft reply into the copy gate (`draftReplies` — Ridhi approves before the client sees anything); at most ONE structured change proposal (`content/admin/change-proposals/<slug>.yaml`, closed-menu validated: only d4 identity fields + registry modules survive `sanitizePatch`, everything else is dropped and reported); and alert escalations (email + ledger) for structural asks, sign-off signals, upset clients, and anything unclassifiable — escalation beats improvisation. **Proposals live behind a new gate**: a Change Proposals panel in the client command center with Approve/Reject; approving applies the patch to the build config (the change loop's single source of truth) and, with `BUILDER_AUTORUN=true` (set on this machine), starts the Builder in the background — apply, QA incl. browser checks, auto-revert on red, ledger report. Rejecting records the decision on the ledger. The engine is deterministic (keyword/pattern extraction: "change the tagline to X", "add a careers page", module hints per sentence); when `ANTHROPIC_API_KEY` is set, an LLM pass (default claude-haiku-4-5, `COMMS_LLM_MODEL` to override) takes over classification + drafting, clamped through the SAME closed-menu validator, with heuristic fallback on any API/parse failure. Verified live on acme-demo: 4-message batch triaged correctly (tagline change + careers module → one proposal; online store → structural alert; question → ack in the draft), dedup on re-tick, mixed valid/invalid patch sanitized with dropped items reported, all-invalid patch refused, reject leaves the config untouched, approve → background Builder → `/careers` live + tagline applied + QA green + commit. Fixed en route: a Builder spawned from inside the Next dev server inherited `NODE_ENV`/`__NEXT_PRIVATE_*` env and failed `next build` — buildAndQa now strips Next-related env (the crash also proved the safety net: change auto-reverted, alert emailed). Worklist now counts pending proposals.
  - Still Ridhi-gated at every exit: replies drafted never sent, configs proposed never edited, pipeline never advanced. The agents propose; Ridhi disposes.
- [x] **[Claude]** Change-implementation loop (done + verified E2E 2026-07-10): the build config is the single source of truth; editing `content/build-configs/<slug>.json` and re-running `npm run builder -- <slug> --report-to <origin>` auto-detects the existing client repo and applies ONLY the delta: identity/theme artifact updates, module payload adds/removes (assembled fresh via Ridhi's own d4-site-builder into a temp dir and content-synced, so generation never drifts), then commits with a descriptive message, re-QAs the real app, and on green pushes (token-gated) + reports to the ledger WITHOUT advancing the pipeline. **QA-rejected changes are automatically git-reverted** back to the last good state + escalated. Manual/Cowork edits elsewhere in the repo survive verbatim (verified). Unchanged config = clean no-op. Runs at `building` or `client-review`; refuses gated stages and dirty working trees. Verified: migration sync, identity change + manual-edit survival, add d4-insights-blog (route live + QA'd), remove d4-careers-portal (payload + nav cleanly deleted), fault injection → auto-revert, no-op detection.
- [x] **[Claude]** APPROVAL + PAYMENT (Wave-less) + HANDOFF flows (done + verified E2E 2026-07-12, walking acme-demo through the entire endgame):
  - **Sign-off flow** (`src/lib/signoff.ts`, Comms): moving a client to `approval` (Ridhi's own action = the authorization) triggers the heartbeat's `signoff-request` duty exactly once: a "Final approval" pending-signoff item appears in the client's portal with an Approve button (the existing mechanism) + a fixed-wording email invitation. The client approving (verified via a REAL portal login + `/api/portal-approve`) records the sign-off (`recordClientSignoff`): ledger `decision` "CLIENT SIGNED OFF... advance to Payment when you're ready" + a state marker. Ridhi still advances the gated stage herself.
  - **Billing watch** (`src/lib/billing.ts`, Billing, `billing-watch` duty): watches the invoices Ridhi records manually on payment-stage clients — nudges exactly once each for (a) at payment with NO invoice recorded, (b) a pending invoice past its dueDate (per-invoice), (c) every invoice paid → "payment gate criterion met, advance to Handoff when ready." All three verified + dedup on re-tick. Wave API later replaces the manual marking; the shape stays.
  - **Handoff package** (`src/lib/handoff.ts` + `/api/agents/handoff` + HandoffPanel in the command center, Concierge): CMS-only both directions (the package holds a live credential — even the agent key can't read it), and generation REQUIRES pipeline = `handoff` (verified refusal at other stages) so the gated stage itself authorizes credential rotation. Generating **rotates the assembled site's ADMIN_PASSWORD** (verified old ≠ new in builds/<slug>/.env.local — the build-time credential dies before the client ever gets one) and writes a markdown handover doc: what-you-own summary, installed modules read from d4.assembly.json, fresh credential + explicit change-on-first-login instruction, and the handoff checklist (rotation pre-checked). Doc lands in `content/admin/handoffs/` which is **gitignored** (verified) — it must never be committed, and the ledger `handoff` entry records everything except the secret (verified no leak). Delivery is Ridhi's, on a channel she trusts; the Workspace panel (visible at handoff/complete) has Generate/Regenerate, View, and Download buttons.
  - Still pending in this area (blocked on Ridhi/credentials): Wave invoice sending + payment webhook detection, GitHub repo ownership transfer (needs the push layer configured), and production deploy/DNS automation (Phase 4).
- [x] **[Claude]** AUTOMATED INVOICING + narrowed gate set (done + verified full-arc E2E 2026-07-12, per Ridhi's instruction: her human touches are internal review and approving each invoice send):
  - **Pricing engine** (`src/lib/pricing.ts` + `content/admin/pricing.yaml`, Ridhi-editable): the quote is built from a **value-anchored price book** (base $2,400; careers $450, blog $400, catalog $600 incl. 50 seeded items, gallery $350; consultations $45/30-min) — the book is also the ceiling, so clients are never charged above what the deliverable is worth. Against it, a **cost floor**: monthly overhead (Claude $20 + Google $30) amortized over expected clients/month, plus build-API/Resend estimates (replaced by recorded actuals when larger) and $8/call ElevenLabs costs. Guardrail: total must clear floor × 3 (configurable) or Billing alerts instead of proceeding.
  - **Cost ledger** (`src/lib/costs.ts`, `content/admin/costs/<slug>.yaml`): per-client actuals (build-api / resend / elevenlabs-call / other) recordable via `/api/agents/billing` action `record-cost` or the panel's "Log a 30-min consultation" button; consultations flow into BOTH the floor and the client's final invoice.
  - **The flow**: deposit invoice (50%) drafts itself once a build config exists and the build has started; client sign-off **auto-advances approval → payment** and drafts the final invoice (balance + post-quote items); Ridhi clicks **Approve & send** on the Billing panel (the one money gate) — the invoice lands on the portal + an itemized email goes out; all-paid **auto-advances payment → handoff and generates the handoff package** (credentials rotated), leaving delivery as the only handoff touch. Overdue invoices alert once each. Rejecting a draft records why; it re-drafts after pricing/config changes.
  - Verified on Meridian Tool & Die end to end: deposit auto-drafted ($1,725 of $3,450, 84× floor), consultation logged mid-project, client portal sign-off → auto payment + final draft ($3,495 − $1,725 = $1,770, consultation included), both sends approved, marked paid → auto handoff + package with rotated password. Worklist counts invoices awaiting send. `docs/agents.md` amended (2026-07-12) with the narrowed gate table; `pipeline.ts` payment stage is no longer Ridhi-gated.
  - Wave integration remains the payment-DETECTION upgrade (replaces manual paid-marking); the drafted-invoice → approve-send flow is unchanged by it.
- [x] **[Claude]** GitHub repo transfer at handoff (2026-07-12, dormant until GITHUB_TOKEN/GITHUB_OWNER): `src/lib/github-transfer.ts` + a `githubUser` field on the client + the Handoff panel's username input and "transfer repo to them" checkbox. Uses GitHub's async transfer API — the client accepts from their side, which is the right shape for a handoff. Refuses gracefully at every missing precondition (no token, no username, repo never pushed).
- [x] **[Claude]** Real (not guessed) API cost tracking, closing a genuine gap in the pricing engine above (2026-07-12): the cost floor was silently running on static guesses (`buildApiCostEstimate`/`resendCostEstimate`) with no actual usage ever measured. Fixed:
  - **Every real Anthropic API call now measures and logs its own exact $ cost** the instant a response comes back — before any parsing of the result can fail, so a call that costs money always gets billed even if the response turns out unusable. `computeAnthropicCost(model, inputTokens, outputTokens)` in `pricing.ts` reads a per-model $/million-token rate table from `pricing.yaml` (`claudeRates`; comment flags it needs checking against Anthropic's live pricing page, since nothing here fetches that live). Sub-cent precision throughout — `costs.ts` rounds to 4 decimals, not 2, so a $0.0045 Haiku call isn't silently zeroed to $0.00.
  - **Model choice, made deliberate rather than uniform**: Comms triage (`src/lib/comms.ts`) drafts actual client-facing reply copy and now defaults to **Sonnet** (`COMMS_LLM_MODEL`); content-seeding extraction (`scripts/seed-content.mjs`) is fact-only, high-volume, and always human-reviewed before `--apply`, so it stays on cheap **Haiku** (`SEED_LLM_MODEL`). The Builder's site assembly is and remains fully deterministic — Ridhi's own `d4-site-builder`, zero LLM calls, zero token cost — so there is no Opus/Sonnet toggle to make there; that's a design property (QA-able, never hallucinates a client's contact info), not a gap.
  - **Real Resend cost**: every client-attributable send (owner-alert-about-client, client reply, credentials, sign-off request, invoice, approval notice) now logs its actual per-email cost (`resendCostPerEmail` in pricing.yaml) the moment it's confirmed delivered — never for the console-logged fallback when no Resend key is configured, which correctly costs nothing.
  - **ElevenLabs stays a manual log** (no phone-consultation feature is built yet — there is nothing to auto-meter); the "Log a 30-min consultation" button remains the honest interim.
  - The existing `max(actual, estimate)` floor logic (unchanged) means real costs only ever push the floor up, never down — verified: hand-calculated Haiku/Sonnet costs from known token counts matched the system's output exactly (down to the fourth decimal), and real per-project totals so far (pennies) correctly stayed under the static estimate, so the floor still conservatively uses the estimate rather than under-billing. BillingPanel now shows the itemized real-cost log (kind, note, precise $) alongside the floor breakdown.

## Phase 7: Maintenance system (after build pipeline is proven)

- [x] **[Claude]** Update loop (done + verified E2E 2026-07-12, deliberately leashed): `npm run maintain -- [<slug>] [--fix]`. Without `--fix` it audits every client repo (`npm audit` + outdated count) and reports to each ledger — critical/high findings alert. With `--fix` it applies `npm audit fix` (NEVER `--force`: breaking upgrades are escalated to a Cowork session, not attempted), commits, runs the FULL QA battery (build + HTTP + browser checks), and keeps the patch only on green — red is git-reverted and node_modules restored. Not on the heartbeat by design: patching runs only when explicitly invoked. Verified with a planted lodash 4.17.20: detected (1 high), patched to 4.18.1, QA green, kept, and the 2 remaining moderate issues honestly flagged as needing a breaking upgrade.
- [ ] **[Claude]** Fleet propagation: canary, verify, gradual rollout, automated rollback, human gate for anything not clean-green (needs >1 shipped client + the deploy pipeline first)
- [x] **[Claude]** Fleet monitoring v1 (done + verified 2026-07-12): `maintenance-watch` heartbeat duty probes every active client with a staging/production URL in status ready/live — two attempts before declaring an outage, alert (ledger + email) only on the up→down transition, a recovery event on the way back, no repeat noise. Verified up → down → recovered on acme-demo. Response-time tracking and multi-region checks are future niceties.

## Phase 8: Rollout

- [ ] **[Both]** Dogfood end-to-end on a throwaway client with every gate manual
- [ ] **[Ridhi]** Relax gates one at a time per the table above as the harness earns trust

## Phase 9: Workspace communications completeness (2026-07-13)

- [x] **[Claude]** Client record gaps closed (verified E2E): `phone` + `internalNotes` (never client-visible) fields on the Client record with Workspace editor fields; the shared **Google Drive folder is the project's file exchange by decision** — contracts and signed documents live there, clients add images/specs/docs there (the portal's message composer now points them to it), so no message-attachment mechanism is needed or built.
- [x] **[Claude]** Consultation records: logging a call now REQUIRES a summary of what was discussed (Billing panel form: minutes + summary) and writes three linked records at once — the consultation record (`content/admin/consultations/<slug>.yaml`), the real cost entry (scaled by duration: 45 min = $12 at the $8/30-min rate), and a ledger event. Invoicing bills per 30-minute block (45 min = 2 × $45 sessions — verified). When the ElevenLabs phone feature is built, its transcript summary lands in the same field through the same function.
- [x] **[Claude]** Email content log (`src/lib/email-log.ts`, `content/admin/emails/<slug>.yaml`): every project email now records its subject + actual text (tags stripped, passwords deliberately omitted from credential emails), delivered or console-fallback alike — "what exactly was the client told?" is always answerable. Wired into all six notify functions alongside the existing cost logging.
- [x] **[Claude]** Unified communications timeline: `/api/agents/timeline` (CMS-only — includes drafts and agent alerts clients must never see) merges messages both directions, pending draft replies, email contents, consultation summaries, sent invoices, and the agent ledger into one chronological view; TimelinePanel renders it in each client's command center with a "hide agent activity" filter. Verified: 22 merged items on Meridian in correct order.
- [x] **[Claude]** Client portal completeness pass (2026-07-13, all verified live through a real portal session): **(1) Payment instructions** — the invoice email promised "payment details are in your portal" while the portal had none; `paymentInstructions` in pricing.yaml now renders as a "How to pay" block under the portal's invoices (with an honest reply-to-your-invoice fallback when unset, and "All settled — thank you!" when nothing is owed), and the email copy is conditional so it never promises what isn't configured. **(2) Itemized invoices** — sent invoices' line items now show in the portal behind a "What's included" expander (server-rendered, no JS), passed through the same client-safe-fields filter as everything else. **(3) "Request a phone call"** button in the message composer pre-fills a structured consultation request; a new `consultation` triage category (checked before the structural patterns, which used to claim "schedule") classifies and escalates these to Ridhi — scheduling stays her permanent gate, and the drafted ack promises only a follow-up. **(4) Triage widened to every active client at any stage** — a consultation request during handoff or an upset message during payment now gets the same triage as one during client review (safe: triage products all stay behind the gates; discovered when a handoff-stage client's messages went silently untriaged).

## Phase 10: Wave billing integration + security pass (2026-07-14)

- [x] **[Claude]** WAVE INTEGRATION (built + verified end-to-end against the real Wave account): `src/lib/wave.ts` — credential-gated (`waveEnabled()` off without `WAVE_ACCESS_TOKEN`+`WAVE_BUSINESS_ID`, and Billing behaves exactly as before). When on, `approveSendInvoice` creates a real Wave invoice (customer auto-created + cached per client, shared "Deneb4 Web Services" product), marks it sent, records the Wave `viewUrl` on the client's portal record, and our branded Resend email carries the pay link. `billingWatchDuty` polls Wave each heartbeat for sent invoices and flips the local record to paid the moment Wave shows settled — **automatic payment detection, the manual "mark paid" step is gone** (manual marking still works as a fallback). Wave failure never blocks a send (falls back to local-only + alerts). Verified live: created a $1 test invoice, queried status, recorded a full manual payment in Wave, confirmed the heartbeat auto-detected it and flipped acme-demo's deposit to paid + logged it, then deleted all test data from the real books (clean). Fixes found by testing: Wave money strings carry thousands separators ("1,800.00" → strip before Number()); personal tokens can't use `invoiceSend` (used `invoiceMarkSent` + our own email instead); manual payments require `exchangeRate`. Wave state (`content/admin/wave/`, customer/product IDs only) is gitignored.
- [x] **[Claude]** SECURITY PASS (audit + fixes, pre-launch hardening — full findings and the launch flip-list in `docs/security-launch-checklist.md`):
  - Timing-safe agent-key comparison across all 8 API routes via a shared `hasAgentKey()` (`src/lib/agent-auth.ts`, `crypto.timingSafeEqual`), replacing `===`.
  - Path-traversal hardening: `isValidSlug`/`assertSafeSlug` guards at every slug→filepath boundary (clients, billing, costs, consultations, email-log, handoff, comms, wave) + clean 400s at the routes that read a slug directly. A `../../.env`-style slug can no longer escape `content/`. Verified with live traversal attempts.
  - `CMS_AUTH_DISABLED` hard-gated to `NODE_ENV !== 'production'` so the dev bypass can never open a production Workspace even if the flag leaks.
  - Confirmed sound (no change): portal/widget scope to session/key not arbitrary slug; ledger channel validator; handoff CMS-only + gitignored; the propose/approve gates.
  - All 8 heartbeat duties healthy after the changes; tsc clean.

## Phase 11: Google Workspace pipeline bridge (2026-07-14, detailed 2026-07-15)

⚠️ **ACCOUNT OWNERSHIP CORRECTION (2026-07-15):** all Google Workspace, GitHub, Vercel, Turso, Resend, Wave, and Anthropic accounts are **business-owned credentials** (Deneb4 LLC's own logins), NOT any individual's personal accounts. Earlier notes in this file and in generated artifacts calling these "personal accounts" were wrong and have been corrected — see the Tech Stack artifact's ownership section.

- [x] **[Ridhi]** Built two real, live Google Apps Script systems in Deneb4's own Google Workspace (outside this repo — Apps Script has no git integration; code lives in the Apps Script project editor, not version-controlled here):
  - **1. Dynamic form generator** (`buildDeneb4IntakeForm`): programmatically builds the master "Deneb4 Project Intake" Google Form from code rather than hand-editing it in the Forms UI. **18 sections** with **conditional page-routing** ("Go To Page" logic) driven by earlier answers — e.g., a "do you need a new domain" answer branches the form differently; wanting a catalog routes through Sections 7A/7B; wanting a careers portal routes through 8A/8B; wanting GitHub code handoff opens its own branch. URL and email fields carry Apps Script's built-in validation so bad data can't reach the sheet. Because the form is generated from code, it can be regenerated/versioned by re-running the script rather than manually re-clicking through the Forms UI — this is also why `parse-intake.mjs` and `intake-webhook.ts`'s Q-code mapping (see below) has to be updated by hand if the generator script changes: there's no shared schema between the two systems today, just a convention (the Q-codes) both sides independently agree on.
  - **2. Client lifecycle & document engine** (`ROUTER_onFormSubmit` + a manual trigger): a genuine 4-phase state machine listening to **three separate Google Forms** (Intake, Onboarding Authorization, Final Approval) plus one manual action, all driving one "Deneb4 Project Matrix" tracking spreadsheet:
    - **Phase 1 — Intake processing & provisioning** (trigger: Intake Form submitted): creates a per-client Drive folder structure (Logos / Photos / Copywriting / Catalog subfolders), generates a customized Google Doc summarizing project scope from the form answers, logs the client into the Project Matrix sheet, and emails the client their portal link plus a request to sign the Authorization Form.
    - **Phase 2 — Scope lock & onboarding sign-off** (trigger: Onboarding Authorization Form submitted): opens the Phase 1 scope doc, appends a digital signature + timestamp, converts it to a locked read-only PDF, **deletes the editable Doc** (the scope literally cannot be altered after this point — not just access-restricted, the editable copy no longer exists), updates status to "In Progress," emails confirmation.
    - **Phase 3 — Manual handoff initialization** (trigger: Ridhi, via a custom menu item in the Sheet itself — `triggerHandoffFromActiveRow`, not a form submission): generates a "Website Final Approval & Handoff" document in the client's Drive folder, updates status to "Pending Handoff," emails the client instructions to review the live site and submit the Final Approval Form.
    - **Phase 4 — Launch authorization & closure** (trigger: Final Approval Form submitted): stamps the Phase 3 handoff document with a final approval timestamp, locks it to PDF, trashes the editable version (same irreversible-lock pattern as Phase 2), updates status to "Project Complete," sends final deployment confirmation email.
  - This is a complete, independently-functioning legal/ops automation (document generation, e-signature-equivalent locking, client communications) that exists whether or not the bridge below is connected — the bridge makes Deneb4's own pipeline aware of it, it does not depend on Deneb4's pipeline to function.
- [x] **[Claude]** THE PIPELINE BRIDGE (built + verified E2E 2026-07-14): her spreadsheet is a bird's-eye tracker; the Deneb4 Workspace pipeline is the granular one. Rather than becoming two separately-maintained sources of truth, her Apps Script now POSTs to a new endpoint at each of her four real milestones, and Deneb4 records the event — WITHOUT taking over her signing mechanism; her e-signed PDFs remain the actual approval instruments.
  - `src/lib/intake-webhook.ts` + `POST /api/agents/intake-webhook` (agent-key only, no CMS-session fallback — this is machine-to-machine). Four events:
    - `intake_submitted`: creates the client if new (mirrors `/api/clients/create`, but returns the portal URL + generated password in the response instead of sending Deneb4's own competing welcome email — so her ONE welcome email carries everything). Parses the raw form answers (Q-code mapping, ported 1:1 from `parse-intake.mjs` — **if the form's codes change, update BOTH files**) and stages a proposed build config for Ridhi's review; never auto-applies it (fact-slot rule + the Phase 0 start-approval gate both still hold). Sets `driveFolder` from the payload (new `setDriveFolderIfEmpty` in clients.ts — never overwrites a manually-set value).
    - `onboarding_signed`: advances pipeline to `intake-review` + alerts Ridhi the staged config is ready for her to review/apply/build. Matched to the client by email (same join key her own script already uses).
    - `handoff_sent`: advances pipeline to `approval` when Ridhi's own toolbar action fires — a mirror, not a replacement; her Handoff Document automation is the real ask.
    - `handoff_signed`: calls the **exact same `recordClientSignoff()`** the portal's own Approve button calls — so approval→payment auto-advance and the final-invoice draft happen through the identical code path, zero new business logic. Verified live: advanced pipeline to `payment` and drafted a real final invoice automatically.
  - Verified end-to-end on a throwaway client through all four events in sequence (create → stage config → apply → sign → handoff-sent → handoff-signed → invoice drafted), plus edge cases (unknown email → 404, wrong key → 401, unknown event → clear error).
  - Apps Script side: one `sendToDeneb4()` helper (Script Properties for the agent key + site URL, `muteHttpExceptions` so a Deneb4 hiccup never breaks her automation) + one call added at each of her four milestones. Snippets handed to Ridhi directly; Apps Script itself isn't in this repo.

## Phase 12: Design pairing system — unique look per client on one skeleton (2026-07-15)

Decision (Ridhi, 2026-07-15): every client keeps the SAME structural skeleton (d4-site-template + modules — layouts stay deterministic and QA-bounded), and per-client uniqueness comes from a **design pairing** layered on top: font pairing + motion signature + mood + fallback palette, chosen as a whole from a closed, labeled menu. Combined with per-client color (brand ingest beats everything) and per-client content, no two active clients should read as siblings. This is the scoped-down realization of the 2026-07-13 "template sets" decision: pairings now, additional full skeletons later only if ever needed.

- [x] **[Claude]** `pairings.json` in d4-site-builder (BUILT + VERIFIED E2E 2026-07-15): the closed menu of 8 pairing sets, each labeled with `voice` / `for` / `avoidFor` descriptions written for agents to match against a client's business type and stated vibe: playful-bright (Fraunces/Nunito Sans, playful-bounce), quiet-luxury (Cormorant Garamond/Karla, still), industrial-confidence (Big Shoulders/IBM Plex Sans, hover-precision), warm-craft (Lora/Work Sans, reveal), modern-signal (Sora/Manrope, reveal-fast — the default), editorial-voice (Newsreader/Source Sans 3, ambient-drift), bold-authority (Barlow Semi Condensed/Barlow, hover-precision-strong), calm-wellness (Spectral/Mulish, reveal-slow). All 8 fallback palettes validated ≥4.5:1 WCAG contrast on every text role against both backgrounds + white-on-accent for buttons. All 16 font families + weights verified against next/font's real font data (gotcha: Google renamed "Big Shoulders Display" → "Big Shoulders").
- [x] **[Claude]** Assembler: build config gains `pairing`; assemble.mjs validates it against pairings.json (closed menu, hard fail on unknown), rewrites `src/config/fonts.generated.ts` (next/font, self-hosted at build) + `src/config/design.generated.ts` (motion mode), applies the pairing's palette when no explicit theme/preset is given (priority: brand-ingested `theme` > `themePreset` > pairing fallback), and records the pairing in d4.assembly.json. AGENTS.md documents the pick-one-whole-pairing rule.
- [x] **[Claude]** Template: `--font-display`/`--font-body` CSS variables wired through globals.css (headings vs body — modules inherit automatically, zero module changes needed); motion layer = `data-motion` on `<html>` + `motion.css` (8 signatures) + `MotionLayer.tsx` (IntersectionObserver scroll-reveal with per-batch stagger). Safety: all motion behind `prefers-reduced-motion: no-preference`; reveal classes added only by JS so no-JS/reduced-motion users always see everything; admin routes excluded. Checked-in defaults = modern-signal so the template runs standalone.
- [x] **[Claude]** Intake: `mapTheme` → `mapDesign` in BOTH `src/lib/intake-webhook.ts` and `scripts/parse-intake.mjs` (the two deliberately-separate ports): Q5-1 visual direction + Q5-2 "words to describe the site" → pairing via ordered keyword rules; a color preset is only pinned when the client explicitly picked a color flavor, otherwise the pairing's validated palette applies. Staged config now carries `pairing` + a note explaining the choice for Ridhi's review.
- [x] Verified end-to-end: assembled a 5-module test client with industrial-confidence → npm install → tsc clean → production build clean → live server: `data-motion="hover-precision"` on html, both font variables applied, Big Shoulders + IBM Plex Sans self-hosted in the served CSS, motion rules shipped. Then swapped the SAME build to playful-bright by regenerating only 3 files (fonts.generated.ts, design.generated.ts, theme.css) → rebuilt → verified Fraunces/Nunito Sans + coral palette + playful-bounce live. A pairing swap is a 3-file diff — cheap for the change loop.
- [ ] **[Ridhi]** Eyeball each of the 8 pairings on a staged build when convenient (they pass QA mechanically; taste is the human gate). Curate: rename/retire/add pairings freely — pairings.json is the single source of truth agents read.

### Phase 12 additions (2026-07-15, second pass)

- [x] **[Claude]** INBOX internal tool in d4-cms-core (verified with a real login): every site with the CMS gets an "Inbox" admin panel showing contact messages and job applications, newest first, with mailto links and job tags. Contact submissions are now ALWAYS stored (previously only when email failed), with email delivery in addition; the request only errors if both channels fail.
- [x] **[Claude]** SEO baseline in d4-site-template: sitemap.xml (routes derived from nav registries) + robots.txt (blocks /admin, links the sitemap). Canonical origin via optional NEXT_PUBLIC_SITE_URL, falling back to the Vercel deployment URL.
- [x] **[Claude]** PAIRING GALLERY artifact for Ridhi's taste review: cycled one real production build through all 8 pairings (3-file swap + rebuild each), screenshotted every home page, published as a private artifact with each pairing's voice/for/avoidFor, fonts, motion, and palette swatches. https://claude.ai/code/artifact/e0205343-4351-48f6-bdfc-39ef7da3145d
- [x] **[Claude]** paymentInstructions filled in pricing.yaml (Wave pay-link wording, per Ridhi 2026-07-15).
- [ ] **[Ridhi]** GOOGLE_CALENDAR_ICS_URL in .env.local is the calendar's web-page URL, not the ICS feed (returns HTML, no events). Fix: Google Calendar → Settings → your calendar → "Integrate calendar" → copy "Secret address in iCal format" (a long URL ending in /basic.ics) and replace the value.
- [ ] **[Ridhi]** For the Vercel + Turso wiring (accounts exist as of 2026-07-15): add to .env.local: `VERCEL_TOKEN=` (vercel.com → Account Settings → Tokens → Create, scope: full) and `TURSO_API_TOKEN=` (Turso dashboard → Account Settings → API Tokens, or `turso auth api-tokens mint deneb4-agents`). These let agents create a Vercel project + Turso database per client. Blob store token comes per-project later.

### Phase 12: deploy automation LIVE (2026-07-15, third pass)

- [x] **[Claude]** `scripts/deploy-client.mjs` — the real per-client deploy, VERIFIED LIVE against Ridhi's actual Vercel + Turso accounts: creates the Turso database `d4-<slug>` (creates the org's group on first-ever use, locations discovered from the API — hardcoded "iad" is invalid, valid codes are aws-* style), mints a DB token, creates Vercel project `d4-client-<slug>`, upserts env (TURSO_DATABASE_URL/TURSO_AUTH_TOKEN; ADMIN_PASSWORD generated once on project creation, printed once, never stored), deploys via Vercel CLI remote build (no local npm install needed), prints the stable production URL last. `--teardown` deletes both resources (test cleanup). Zero AI cost, all deterministic API calls.
- [x] Live test result: throwaway client deployed to a real public URL; home/careers/admin all 200; `/api/careers` read `{"jobs":[]}` from the real Turso DB; contact + application POSTs both wrote to it (`ok:true`; those routes 500 on store failure, so this is proof of real serverless→Turso writes); robots.txt correct. Vercel project + Turso DB then deleted; accounts left clean (the `default` Turso group remains, correctly — all future client DBs use it).
- [x] `STAGING_DEPLOY_CMD` in `.env.local` now points at deploy-client.mjs, so the existing staging flow (`deploy-staging.mjs`, AUTO_DEPLOY_STAGING, the ledger writeback) drives real public deploys with zero further changes. `.env.example` documents VERCEL_TOKEN/TURSO_API_TOKEN.
- [ ] **[Ridhi]** Calendar ICS URL second attempt still wrong (now 404s): needs the SECRET address — Google Calendar → Settings → your calendar → "Integrate calendar" → "Secret address in iCal format" (long URL ending /basic.ics). The "public address" variant 404s unless the calendar is public.
- [ ] **[Both]** OPEN QUESTION from Ridhi's pairing-gallery review: she describes "our skeleton" as having a mega menu and hand-built features — that is NOT d4-site-template (simple top nav; the gallery screenshots ARE real builds of the current skeleton). Likely her Codex template work. Need: where it lives (repo/folder), then decide integrate-as-new-base vs. add-as-second-set.

## Phase 13: next-gen skeleton from the Eagle shell (started 2026-07-15)

Ridhi's direction: clone reagle2838/eagle-website (a real client's live repo — NEVER touch the original) and use the clone as the basis for the new skeleton. Clone secured at `vendor/d4-shell/` (gitignored, fresh single-commit git history, all 326 files verified). Inventory: Next 15 / React 19 / Tailwind 3 (same generation as d4-site-template), ~17.5k lines, 1,049-line mega-menu Navbar, dark/light ThemeToggle + ThemeContext, AnnouncementBar, QuoteButton/QuoteModal, FaqAccordion, ClientMarquee, ProductCard/ProductImage, TipTap-based rich text editors, Supabase data layer, own admin ("eagle-ops"), 117MB of Eagle's real assets.

THE PLAN (graft, not wholesale swap — a direct swap would bake Eagle's business pages/content/assets into every client and replace the module + Turso contracts):
- [x] **[Claude]** DONE 2026-07-15 — Ported the SHELL into d4-site-template v1.1.0, genericized: mega-menu Header (config-driven `nav.base`/`nav.tail` NavItem trees with `description` + `children`, max two levels of nesting; one level = description-row panel, two levels = category-rail panel à la Eagle's Siemens layout; flat items stay plain links; module nav entries merge in unchanged), MegaPanel (backdrop, panel-top tracking, Esc/route-change close, focus trap, CTA sidebar), nav-driven Footer (group columns + ↳ sub-links, social icons via `socialLinks` config, contact block), AnnouncementBar (config-gated `announcement`, session dismiss), QuoteModal + QuoteButton (site-wide `d4:open-quote-modal` event seam, posts to the template's own /api/contact which gained optional company/phone/topic), FaqAccordion, LogoMarquee (props-driven, CSS marquee, reduced-motion safe), ProductCard/ProductImage (generic ProductCardItem props, quote CTA option, shimmer). Everything re-tokened to theme.css variables; `.btn-primary`/`.btn-outline` component classes added.
- [x] **[Claude]** DONE 2026-07-15 — Dark/light mode: `darkMode: "class"` + `.dark` token block in theme.css; NEW `--accent-contrast` token (text on accent fills — white in light, near-black ink in dark, so dark accents can be light readable tints); self-contained ThemeToggle (localStorage `d4-theme`, gated on `darkMode` export in design.generated.ts) + pre-paint inline script in layout; dark palettes for ALL 8 pairings (`fallbackThemeDark` in pairings.json v2) and all 3 presets (moved from assemble.mjs inline to presets.json, light+dark); `bin/validate-contrast.mjs` validator — 264 checks green (also caught+fixed warm-sand's pre-existing 4.41:1 white-on-accent). Custom `theme` without `themeDark` ships light-only by design (never un-validated dark); `darkMode:false` config override. Assembler writes `.dark` block + darkMode flag; d4.assembly.json records darkMode; SYNCED_ARTIFACTS in d4.mjs gained fonts.generated.ts + design.generated.ts (pairing/dark swaps now propagate through the change loop). Template pages swept: `text-white` on accent → `text-on-accent`.
- [x] NOT ported (as planned): Eagle's business pages, all Eagle content + assets, Supabase layer, eagle-ops admin, their Reveal component. Eagle-specific mega-menu features intentionally dropped: in-menu search, fetched-article rails, featured-content cards (content-module territory, not shell).
- [x] **[Ridhi]** DONE 2026-07-15 — PAT re-minted as deneb4admin (repo scope). Eagle snapshot pushed to PRIVATE deneb4admin/d4-shell (token scrubbed from the remote afterward). Builder repo creation LIVE: deneb4admin/d4-client-acme-demo and d4-client-meridian-tool-and-die created private and pushed; future builds/changes push automatically. Guard kept: maybePushToGitHub verifies token identity vs GITHUB_OWNER and refuses on personal-account mismatch. Also fixed: the tokened push now runs with credential.helper disabled and resets origin to the clean URL, because GCM was caching the x-access-token credential and shadowing the user's own GitHub login machine-wide (this briefly broke pushes to the main repo; stray credential erased).
- [x] Pairing gallery artifact refreshed 2026-07-15 with the v1.1.0 shell: all 8 pairings shot from real production builds (Harbor & Pine Studio demo) in light mode, dark mode, and with the mega menu open, plus a quote-modal detail shot.
- [ ] Follow-ups: browser-QA contrast bars run per-mode only for the light default today — add a dark-mode axe pass (toggle .dark) to verify.mjs browser battery; consider porting ProductCard adoption into d4-catalog's list UI; LogoMarquee/FaqAccordion are shell components awaiting a config/content seam (module or page templates) to surface per-client.

## Phase 14: Maximum agency — agents as the logic gates (mandated by Ridhi 2026-07-15)

Ridhi's directive, verbatim intent: "I just want this to be as agentic as possible." The Google Apps Script
automations and the Deneb4 agents must communicate BOTH ways, so that agents — not a human — are the logic
gates between pipeline steps. Human-in-the-loop shrinks to exactly FOUR touchpoints:

1. **Quote approval (Ridhi first, then client, BEFORE any build starts).** Agent drafts the quote from the
   intake → Ridhi approves or DENIES WITH INSTRUCTIONS (a feedback loop: her notes go back to the agent,
   which revises the quote/config and resubmits — not just a reject button) → only after her approval does
   it go to the client for confirmation.
2. **Deposit paid (client).** The 50% upfront invoice must be PAID before the build begins — payment is the
   trigger that starts the Builder, not a stage Ridhi advances.
3. **Invoicing approvals (Ridhi).** Approve-and-send on every invoice stays hers.
4. **Final approval of the website (Ridhi AND the client, jointly).** The one look-at-the-site gate.

Everything else becomes agent-driven: intake config auto-applies after quote+deposit clear, build auto-starts
on deposit payment, the internal-review gate folds into the joint final approval, and the GAS document engine's
manual steps (e.g. the sheet-menu handoff trigger) become agent-callable.

- [x] **[Claude]** DONE 2026-07-16 — Deneb4 → GAS direction of the bridge: `src/lib/gas-bridge.ts` + outbound calls from agents
      to a GAS Web App endpoint (env: GAS_WEBAPP_URL + GAS_SHARED_SECRET). Lets agents trigger what today needs
      Ridhi's click in the Sheet: send-handoff-document, Matrix row updates, scope-doc actions. GAS → Deneb4
      already exists (the 4-event webhook, Phase 11).
- [ ] **[Ridhi]** Deploy the Apps Script project as a Web App (doPost handler — code to be supplied when the
      bridge is built) and put the shared secret in Script Properties. Prereq for the direction above.
- [x] **[Claude]** DONE 2026-07-16 — Quote gate with feedback loop: quote drafts at intake-review; PENDING_RIDHI → (approve |
      deny+instructions → agent revises → resubmit) → PENDING_CLIENT → client confirms → deposit invoice sends
      (her invoice-send click may be folded into her quote approval — one click, not two; decide at build).
- [x] **[Claude]** DONE 2026-07-16 — Deposit-starts-the-build: pipeline reorder so `building` is entered automatically on
      deposit settlement (Wave detection already live), never before.
- [x] **[Claude]** DONE 2026-07-16 — Gate relaxation per the four-touchpoint list: auto-apply staged configs post-quote,
      remove internal-review as a separate human stage, auto-apply QA-green change proposals. Structural
      (off-menu) requests STILL escalate — closed catalog unchanged.
- [x] **[Both]** docs/agents.md amendment WRITTEN 2026-07-16 (Ridhi: re-approve it) (this mandate supersedes the 2026-07-08 gate table;
      re-approve the amended doc).
- [ ] **[Ridhi — decisions needed before build]** (a) Comms replies: your four-touchpoint list implies drafted
      replies AUTO-SEND without your review — confirm that's intended (urgent/upset/structural still escalate).
      (b) Handoff delivery isn't in your HITL list: auto-email the credentials package on final payment, or keep
      the manual delivery step? (c) Client quote confirmation mechanism: portal Approve button, or a GAS
      Authorization Form like your other sign-offs (recommended: GAS form → it e-signs and PDF-locks the quote
      exactly like the scope doc, and the existing webhook reports it back).
- [x] **[Claude]** DONE 2026-07-16 — Questionnaire wiring for the shell (spec = the Intake Questionnaire artifact,
      2026-07-15): promote Q6-2 (primary visitor action → header/hero CTA + quote.enabled); add + map quote
      topics, announcement, social links, FAQ pairs, logo list, billing contact, per-module upload prompts.
      Update BOTH mapping copies (parse-intake.mjs + intake-webhook.ts) and the form generator.
- [x] **[Claude]** DONE 2026-07-16 (template v1.2.0) — Page seams for FaqAccordion + LogoMarquee (components shipped in v1.1.0; no page renders
      them yet) — config/content-driven sections so the new intake questions have somewhere to land.
- [ ] **[Both]** PREREQ for all of Phase 14 in production: the studio app itself needs a public production
      deploy (Vercel) — GAS can only webhook a public URL, and the E2E bridge verification so far ran locally.

- [x] **[Claude]** Phase 14 core VERIFIED E2E 2026-07-16 (31/31 checks, live dev server): intake → shell-config staging (quote topics/announcement/social/FAQ/logo wall) → auto-quote → deny-with-instructions ("remove the blog and take $200 off" removed the module AND applied -$200) → Ridhi approve → portal Quote-approval item + email → client Approve → config applied + adjustment-aware deposit drafted → send → paid → tick auto-advanced to building → comms auto-sent a clean question's reply while a consultation request stayed a draft → /api/portal-handoff auth (401 anon, 404 pre-package). Engineering decisions made under the mandate, flagged for Ridhi's veto: (a) COMMS_AUTOSEND on with escalation carve-outs, (b) handoff package delivered via authenticated portal download + no-secrets email (credentials never emailed), (c) client quote confirmation = portal Approve AND/OR a future GAS Quote Authorization Form (quote_signed event, snippet in docs/gas-bridge.md).
- [ ] **[Ridhi]** Phase 14 activation: deploy the Apps Script as a Web App + set GAS_WEBAPP_URL/GAS_SHARED_SECRET (docs/gas-bridge.md, ~5 min); optionally add the Quote Authorization Form (recommended) with a quote_signed call; re-approve docs/agents.md; production deploy of the studio app remains the prerequisite for the bridge outside local testing.

## Phase 15: client self-service + own intake form (2026-07-16)

- [x] **[Claude]** Password self-service: `/api/portal-change-password` (authenticated, requires current password) + `/api/portal-forgot-password` (public, generic-success, reuses the tested credentials-reset email). Welcome email now tells the client both exist.
- [x] **[Claude]** Deneb4's own intake form at `/intake` (+ `/api/intake`), replacing the Google Form as the primary intake path — built from the Intake Questionnaire Spec artifact, submits directly into the existing `handleIntakeSubmitted`/staging/quote-draft pipeline (zero new parsing logic). The Google Form + GAS webhook path (Phase 11) keeps working unchanged for anyone still on it.
- [x] **[Claude]** Bug found + fixed via live smoke test: the brand-URL fallback in both intake parsers scanned every response for anything URL-shaped, so a social link got misread as the client's existing website when the dedicated field was blank. Fixed in both copies.
- [x] **[Claude]** DONE 2026-07-16 — Scope agreement + e-signature brought in-house (src/lib/scope.ts): generated from the confirmed quote, signed by the client (typed name) in the same portal action as confirming the quote, locked on signature (immutable — a later config change starts a new version), served via authenticated /api/portal-scope download. Decision made by Ridhi: simple in-house click-to-sign, not a paid e-signature API. Verified live end-to-end; one bug found + fixed (duplicate approve calls before either was signed → made scope generation idempotent).
- [x] **[Both]** DONE 2026-07-16 — Client file uploads in-house (Ridhi provisioned the Blob token; Claude built /api/portal-upload + UploadWidget in the portal Files section, categories Logos/Photos/Copywriting/Catalog). Live-tested against the real store, which turned out to be PRIVATE (correct for client business docs) — so files are served exclusively through /api/portal-file, an authenticated streaming proxy (owning client's portal session or the CMS session, path-scoped per client), never bare blob URLs. ⚠️ Gotcha: the token is QUOTED in .env.local — Next strips quotes, raw-parsing scripts must too.
- [x] **GAS DOCUMENT-ENGINE PARITY COMPLETE (2026-07-16).** Everything the Apps Script system did now runs in-house: intake form (/intake), scope agreement + click-to-sign e-signature (scope.ts, locked on signing), client file uploads (Blob + authed proxy), plus the pipeline/quote/billing automation that already existed. Deneb4's code has ZERO remaining dependency on Google Apps Script, Forms, or the Project Matrix sheet.
- [x] **[Ridhi]** DONE 2026-07-16 — Old Google Form unpublished and all Apps Script triggers removed. Google Workspace is now fully out of the client pipeline. Vestigial code kept deliberately for now (harmless, all dormant): the inbound webhook route (/api/agents/intake-webhook — still useful as a generic machine-to-machine intake path), gas-bridge.ts outbound calls (no-ops without GAS_WEBAPP_URL), and docs/gas-bridge.md (historical). Candidates for removal in a future cleanup pass.

## Standing direction: Stardrive (recorded 2026-07-16)

Ridhi bought **stardrive.dev**: the future home of "Stardrive Technology," the productized/licensed version of the d4 engine (the Licensing Feasibility artifact's Model A→B path). Deneb4 stays Deneb4 on deneb4.com — the studio serving its own clients. Stardrive is the engine as a sellable product for other freelancers/agencies. NOT a current build priority; the standing instruction is architectural: keep building so the engine/studio separation stays clean —
- Keep the d4 ecosystem repos (Layer 2) free of anything Deneb4-specific (pricing, client data, studio branding) — they are the future Stardrive product.
- Prefer manifest/token/contract-driven mechanisms over hardcoded studio behavior; every hardcoded assumption is future Stardrive porting work.
- When adding studio-app features, note (in code comments or here) which parts are "engine" vs "Deneb4's own operation" — the intake field-mapping layer, tenant credentials, and per-licensee registries from the licensing artifact remain the known gaps.


## Phase 16: the Engineer agent + one-click heartbeat (2026-07-16)

- [x] **[Claude]** One-click heartbeat scheduling: "Enable auto-runs (30 min)" button on the Agents tab replaces the manual PowerShell command — /api/agents/heartbeat-schedule (CMS-session only; registering a scheduled task is a machine change) wraps register-heartbeat-task.ps1 with the app's own origin as the tick URL. Windows-only by design; PRODUCTION heartbeat is now also wired: vercel.json cron (*/30) hits /api/agents/tick GET, authenticated by the CRON_SECRET Vercel injects — zero machine setup once the studio app is deployed.
- [x] **[Ridhi decision]** Claude Cowork is OUT as the escalation path; docs/agents.md amended: the escalation role becomes "the Engineer," an in-house agent (work-order-only, commit+QA+auto-revert doctrine, escalates-back-with-branch on uncertainty). Interim: supervised sessions continue with identical mechanics.
- [x] **[Claude]** BUILT + E2E VERIFIED 2026-07-16 — the Engineer agent. Honest scoping: this is the hardest agent in the roster — an unattended coding agent that safely modifies client repos. Sane v1: Anthropic API-driven, scoped to a WORK ORDER (Ridhi writes the spec), operating on a branch of the client repo, full QA battery gate, auto-revert on red, PR-style summary back to Ridhi, hard cap on files touched. Build AFTER the studio has real paying-client traffic; the interim path costs nothing.

## Phase 17 (backlog): Stardrive — the engine as installable software (recorded 2026-07-16; DELIVERY SHAPE SUPERSEDED by the Phase 18 API-first pivot — the engine/contract analysis below still stands)

Ridhi's product vision, refined: **Stardrive is desktop software** a freelancer/agency purchases and installs — a real interface where they upload their OWN templates (the d4 template catalog ships included), fill in a client's facts, and it assembles/QAs/deploys exactly like Deneb4's builder. Domain purchased: stardrive.dev.

Feasibility: YES — precisely because the engine is deterministic, file-based Node with no hosted dependency (local SQLite dev fallback everywhere). The realistic shape:
- **Shell**: Electron (pragmatic choice — the whole engine is Node; Tauri would need a Node sidecar anyway). UI = the "workbench": template library (d4 catalog + imported third-party templates validated against manifest.schema.json + validate-contrast), client-facts form, assemble/preview/QA/deploy buttons.
- **Reuses as-is**: d4-site-builder (assembler, registry, pairings, validators), d4-site-template + modules, the QA harness (verify.mjs — Playwright bundling adds install weight but works), deploy-client.mjs pattern (licensee brings their OWN Vercel/Turso/GitHub tokens, entered in the app's settings).
- **Must be built new** (matches the licensing artifact's known gaps): the declarative intake field-mapping layer (licensee's own questionnaire → their template's config slots), per-licensee template registry, the desktop UI itself, license activation (key file; sell via Paddle/Lemon Squeezy — they handle sales tax), installer packaging + code signing (Windows cert + Apple notarization = real cost + paperwork), template-author docs (Phase 1 of the licensing artifact).
- **Explicitly OUT of v1**: the agent system, portal, pipeline, billing — those are Deneb4-the-studio's ops layer, not the builder. "Works exactly as Deneb4's website builder does" = the BUILDER, which is exactly the part that's engine.
- **Estimate (honest, order-of-magnitude)**: workbench MVP usable by a friendly beta licensee ≈ 15–25 focused build sessions (assembler UI, field-mapping, template import + validation, packaging). Sellable 1.0 (licensing, signing, docs, stardrive.dev site, payments) ≈ 2–3× that. Calendar time depends entirely on pace alongside Deneb4's own client work.
- **Repos**: keep the d4-* engine repos exactly where they are (they ARE the product content). NEW repo for the app itself — recommend a fresh "stardrive" GitHub org (clean IP boundary for a product that may be sold/licensed) with the app repo private until launch. stardrive.dev marketing site: assemble it WITH the d4 engine (dogfood + demo in one).
- **Sequencing**: after Deneb4 has real paying clients (Ridhi's own stated priority). First concrete step when ready: the field-mapping layer — it's also the licensing artifact's Phase 2 and benefits Deneb4 directly.

- [x] **Engineer v1 facts (2026-07-16):** src/lib/engineer.ts (work-order store content/admin/work-orders/<slug>.json + merge/reject/pushMain git ops) · /api/agents/engineer (create/approve/reject CMS-only; update = runner's agent-key callback) · scripts/engineer.mjs (branch → Claude Agent SDK coding engine [ANTHROPIC_API_KEY required; ENGINEER_MODEL override; --engine dry for keyless pipeline tests] → caps [≤40 files, .env*/.git forbidden] → commit → compact QA on :4184 → review/failed) · EngineerPanel in ClientCommandCenter. Gate kept deliberately: Approve & merge is Ridhi's click; QA-green-only reaches her; failures keep their branch. E2E on acme-demo: create→branch→edit→commit→QA green→review→approve→no-ff merge→REAL GitHub push; dirty-tree guard fast-fails; reject deletes branch. Drill reverted+pushed after. ⚠️ ANTHROPIC_API_KEY is NOT yet in .env.local — the dry engine proved the pipeline; the real coding engine activates when Ridhi adds the key (this also upgrades comms/seeding from heuristics, which have been carrying those seams all along).
- [x] **Stardrive M0 (2026-07-16):** token verified (identity stardrivedev = owner, User account), private repo stardrivedev/stardrive-workbench created + pushed (token scrubbed from remote). Scaffold at Desktop/Stardrive/stardrive-workbench: README (vision/architecture), docs/ROADMAP.md (M0–M4 with session estimates), docs/template-author-contract.md (manifest + theme-token + admin-panel contracts productized), proprietary LICENSE placeholder. Next Stardrive step = M1 field-mapping layer, built inside Deneb4 first then extracted.

## Phase 18: Stardrive M1 (field mapping) + API-first pivot — DONE 2026-07-16

- [x] **[Ridhi decision]** Stardrive delivery pivots from Electron desktop software to **API-first**: the API key is the product (usage-metered, recurring), the "Workbench" becomes a thin web dashboard over the same API, and the entire installer/code-signing/notarization cost class from Phase 17 is eliminated. Rationale: agencies integrate pipelines rather than click GUIs; recurring beats one-time; hobbyists still get the dashboard. Known trade-off (recorded): no offline story, Stardrive absorbs build compute; a desktop wrapper can return post-beta if demanded. Spec: stardrive-workbench/docs/api-design.md (v1 REST surface: mappings/templates/sites/deploy, key scoping, metering shape).
- [x] **[Claude]** **Stardrive M1 BUILT + Deneb4 runs on it**: packages/field-mapping — a pure-ESM, zero-dependency declarative engine (runMapping/validateMapping + shared text helpers, index.d.mts types, full authoring spec in its README, worked example examples/coffee-cart.json, 21 unit tests in test/run.mjs). Deneb4's intake mapping is now ONE file — content/admin/intake-mapping.json (stardrive-field-mapping/v1) — executed by BOTH former hand-synced parsers: src/lib/intake-webhook.ts (live /intake form + webhook path; parseAndStage is now ~20 lines of glue) and scripts/parse-intake.mjs (CSV path; wrapper keeps stage/preview/apply/discard CLI). When the questionnaire changes, edit the mapping — no parser code, no drift.
- [x] **[Claude]** Verification: golden-parity regression — scripts/test-field-mapping.mjs replays fixtures (scripts/fixtures/field-mapping/{rich,sparse}.json) against goldens captured from the ORIGINAL parser (verbatim replica of f35e557 parseAndStage) — config/contact/flags/notes(text+order)/unmapped/context/mapReport all deep-equal (sole sanctioned diff: config.$comment provenance wording, no longer mentions Google Forms). Same parity proven on the CSV CLI (full question-title headers) and LIVE on a spare-port dev server: POST /api/intake → staged file === golden (RESEND_API_KEY blanked for the smoke; all test artifacts — client/ledger/email-log/quote/intake — removed after). npx tsc clean. Engine tests green standalone in the Stardrive repo too (pushed as f8ca4d0). Note for the next questionnaire change: the /intake form (IntakeForm.tsx) still posts fixed Q-codes — update form + mapping together; mapReport makes a mismatch loud.
- [x] Standing rule recorded in both repos: packages/field-mapping develops in the Deneb4 repo (where golden fixtures live), extracts to stardrive-workbench on change — one direction only.
- [x] **Stardrive M2 FOUNDATION SHIPPED (2026-07-16, stardrive-workbench 28cf396):** services/api — the Stardrive API v1, zero runtime deps (node:http + file-backed var/ state, Turso-swappable interface). Live + E2E-tested (24 checks over real HTTP): hashed/scoped/revocable sk_live keys with timing-safe compare, per-key rate limiting (429 + Retry-After), per-key monthly metering (failures never billed), mappings validate/parse/CRUD (the M1 engine as a service), the six d4 manifests bundled with boot self-validation + a full-report manifest validator (licensee template names free-form; d4- prefix is first-party only), and the full dry site/job lifecycle (assemble from config or mappingId+answers, restart-safe jobs, change loop with config history; dry QA recorded as skipped, never passed). Import/deploy/export = honest 501s. M2 remainder: real engine executor (jobs.mjs only — needs the engine-repos packaging decision), deploy with licensee tokens, export, template import + palette contrast, webhooks, Turso store, container, CORS.
